apiVersion: orchestrator.aiops.ibm.com/v1alpha1
kind: Installation
metadata:
  name: {{ current_ibm-aiops_feature.aiops_name }}
  namespace: {{ current_ibm-aiops_cluster.project }}
spec:
  imagePullSecret: ibm-entitlement-key
  license:
    accept: true
  pakModules:
    - enabled: true
      name: aiManager
    - enabled: true
      name: aiopsFoundation
    - enabled: true
      name: applicationManager
    - enabled: true
      name: connection
  size: {{ current_ibm-aiops_feature.aiops_size }}
  storageClass: '{{ AIOPS_STORAGE_CLASS_FILE }}'
  storageClassLargeBlock: '{{ AIOPS_STORAGE_CLASS_LARGE_BLOCK }}'

